{
  "name": "pnm-cli",
  "version": "0.0.1",
  "description": "A command line tool for creating private zones and checks",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gaurav Bajaj",
  "license": "ISC",
  "bin": {
    "pnm-cli": "./index.js"
  },
  "dependencies": {
    "commander": "^2.9.0",
    "request": "^2.80.0"
  }
}
